import {
  createContext,
  ReactNode,
  useState,
  useReducer,
  useEffect,
} from 'react'

interface CartContextProvaiderProps {
  children: ReactNode
}

export function CartContextProvaider({}: CartContextProvaiderProps) {
  // const[cartState, dispatch] = useReducer()
}
